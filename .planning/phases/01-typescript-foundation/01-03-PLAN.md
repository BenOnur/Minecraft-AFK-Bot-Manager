---
phase: 01-typescript-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/utils/Auth.ts
  - src/commands/CommandParser.ts
autonomous: true
requirements:
  - TS-04
  - TS-05
must_haves:
  truths:
    - Auth utility is fully typed with proper config typing
    - CommandParser has full type coverage
  artifacts:
    - path: "src/utils/Auth.ts"
      provides: "Typed authentication/authorization utility"
    - path: "src/commands/CommandParser.ts"
      provides: "Typed command parsing utility"
  key_links:
    - from: "src/utils/Auth.ts"
      to: "src/types/index.ts"
      via: "import BotConfig"
    - from: "src/commands/CommandParser.ts"
      to: "src/types/index.ts"
      via: "import ParsedCommand, SlotValidation"
---

<objective>
Type the Auth and CommandParser utilities.

Purpose: Complete TypeScript typing for the core utility modules used by the bot.
Output: src/utils/Auth.ts and src/commands/CommandParser.ts with full type coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-typescript-foundation/01-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

From src/utils/Auth.js:
- Class Auth with constructor accepting config
- Methods: isTelegramUserAuthorized(userId), isDiscordUserAuthorized(userId, guildId)

From src/commands/CommandParser.js:
- Static methods: parseSlots(slotString), parseCommand(text), validateSlots(slots, availableSlots)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Auth.js to TypeScript</name>
  <files>src/utils/Auth.ts</files>
  <action>
Convert src/utils/Auth.js to src/utils/Auth.ts:

1. Import types from src/types:
   ```typescript
   import type { BotConfig } from '../types/index.js';
   import logger from './Logger.js';
   ```

2. Type the Auth class:
   ```typescript
   export class Auth {
     private readonly telegramAllowedUsers: string[];
     private readonly discordAllowedUsers: string[];
     private readonly discordGuildId: string | null;

     constructor(config: BotConfig);
     
     isTelegramUserAuthorized(userId: string | number): boolean;
     isDiscordUserAuthorized(userId: string | number, guildId?: string): boolean;
   }
   ```

Note: userId can be string or number (Telegram user IDs are numbers, Discord are strings). Handle both.
  </action>
  <verify>
<automated>npm run typecheck -- src/utils/Auth.ts 2>&1 | head -30</automated>
  </verify>
  <done>Auth.ts compiles without errors, all methods typed</done>
</task>

<task type="auto">
  <name>Task 2: Convert CommandParser.js to TypeScript</name>
  <files>src/commands/CommandParser.ts</files>
  <action>
Convert src/commands/CommandParser.js to src/commands/CommandParser.ts:

1. Import types:
   ```typescript
   import type { ParsedCommand, SlotValidation } from '../types/index.js';
   import logger from '../utils/Logger.js';
   ```

2. Type the CommandParser class:
   ```typescript
   export class CommandParser {
     // 'all' means drop all slots
     static parseSlots(slotString: string): number[] | 'all';
     
     static parseCommand(text: string): ParsedCommand;
     
     static validateSlots(
       slots: number[] | 'all', 
       availableSlots: number[]
     ): SlotValidation;
   }
   ```

3. Add type guards for the 'all' case in validateSlots:
   - If slots === 'all', return all available slots
  </action>
  <verify>
<automated>npm run typecheck -- src/commands/CommandParser.ts 2>&1 | head -30</automated>
  </verify>
  <done>CommandParser.ts compiles without errors, all methods typed</done>
</task>

</tasks>

<verification>
npm run typecheck passes for all TypeScript files
</verification>

<success_criteria>
- src/utils/Auth.ts compiles without errors
- src/commands/CommandParser.ts compiles without errors
- All utility functions have proper return types
- npm run typecheck works
</success_criteria>

<output>
After completion, create `.planning/phases/01-typescript-foundation/01-03-SUMMARY.md`
</output>
