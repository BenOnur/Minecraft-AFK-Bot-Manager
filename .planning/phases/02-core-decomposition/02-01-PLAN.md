---
phase: 02-core-decomposition
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/CommandHandler.ts
  - src/BotManager.ts
autonomous: true
requirements:
  - TS-06
  - TS-07
must_haves:
  truths:
    - CommandHandler utility is fully typed
    - BotManager is fully typed
  artifacts:
    - path: "src/commands/CommandHandler.ts"
      provides: "Typed command handler"
    - path: "src/BotManager.ts"
      provides: "Typed bot manager"
---

<objective>
Type CommandHandler and BotManager utilities.

Purpose: Add TypeScript types to CommandHandler and BotManager for better type safety.
Output: src/commands/CommandHandler.ts, src/BotManager.ts
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-typescript-foundation/01-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

From src/commands/CommandHandler.js:
- Has handleCommand, handleSay, handleAll, handleStatus, etc.
- Returns { success: boolean, message: string, data?: any }

From src/BotManager.js:
- Manages multiple MinecraftBot instances
- Has telegramBot, discordBot references
- Account management methods
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type CommandHandler.js to TypeScript</name>
  <files>src/commands/CommandHandler.ts</files>
  <action>
Convert src/commands/CommandHandler.js to TypeScript:

1. Import types:
   ```typescript
   import type { BotConfig } from '../types/index.js';
   ```

2. Add proper types for BotManager reference
3. Type all handler methods with proper return types:
   ```typescript
   interface CommandResult {
     success: boolean;
     message: string;
     data?: unknown;
     parseOptions?: { parse_mode: string };
     type?: string;
     // ...
   }
   ```

4. Export as typed class
  </action>
  <verify>
<automated>npm run typecheck 2>&1 | head -30</automated>
  </verify>
  <done>CommandHandler.ts compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Type BotManager.js to TypeScript</name>
  <files>src/BotManager.ts</files>
  <action>
Convert src/BotManager.js to TypeScript:

1. Import types:
   ```typescript
   import type { BotConfig, MinecraftAccount } from '../types/index.js';
   ```

2. Add proper types for:
   - telegramBot: TelegrafBot | null
   - discordBot: DiscordBot | null
   - bots: Map<number, MinecraftBot>

3. Type all methods with proper return types

4. Export as typed class
  </action>
  <verify>
<automated>npm run typecheck 2>&1 | head -30</automated>
  </verify>
  <done>BotManager.ts compiles without errors</done>
</task>

</tasks>

<verification>
npm run typecheck passes for all TypeScript files
</verification>

<success_criteria>
- src/commands/CommandHandler.ts compiles without errors
- src/BotManager.ts compiles without errors
- All methods have proper return types
- npm run typecheck works
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-decomposition/02-01-SUMMARY.md`
</output>
