---
phase: 02-core-decomposition
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/MinecraftBot.ts
autonomous: true
requirements:
  - TS-08
must_haves:
  truths:
    - MinecraftBot is fully typed
  artifacts:
    - path: "src/MinecraftBot.ts"
      provides: "Typed main bot class"
---

<objective>
Type MinecraftBot class.

Purpose: Add TypeScript types to the main MinecraftBot class.
Output: src/MinecraftBot.ts
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-decomposition/02-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

From src/MinecraftBot.js:
- Very large file (1600+ lines)
- Has connection management, anti-AFK, protection, inventory, AFK profile
- Uses mineflayer library
- Has many internal states and intervals
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type MinecraftBot.js to TypeScript</name>
  <files>src/MinecraftBot.ts</files>
  <action>
Convert src/MinecraftBot.js to TypeScript:

1. Import types from mineflayer (use existing @types/mineflayer)
   ```typescript
   import type { Bot } from 'mineflayer';
   import type { BotConfig, MinecraftAccount, AfkProfile, Position } from './types/index.js';
   ```

2. Define interface for bot options, stats, etc.

3. Type constructor parameters:
   ```typescript
   constructor(config: BotConfig, accountConfig: MinecraftAccount);
   ```

4. Type all public methods with proper return types

5. Use `// eslint-disable-next-line @typescript-eslint/no-explicit-any` where mineflayer types are complex

6. Export as typed class
  </action>
  <verify>
<automated>npm run typecheck 2>&1 | head -50</automated>
  </verify>
  <done>MinecraftBot.ts compiles without errors</done>
</task>

</tasks>

<verification>
npm run typecheck passes for all TypeScript files
</verification>

<success_criteria>
- src/MinecraftBot.ts compiles without errors
- All methods have proper return types
- npm run typecheck works
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-decomposition/02-02-SUMMARY.md`
</output>
